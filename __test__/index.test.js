const Print = require("../index");

describe("Print Class", () => {
  // it("Should throw error  when intialized if the value passed is not a boolean", () => {
  //   let debug = "";
  //   let errorM = "Print.constructor: Must be a boolean.";
  //   expect(() => new Print(debug)).toThrow(new Error(errorM));
  // });
  // it("should console log when ");
});
